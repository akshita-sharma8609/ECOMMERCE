const { addProduct } = require("./addProduct");
const { addProductItems } = require("./addProductItems");
const { getProductItems } = require("./getProductItems");

module.exports = {
  addProduct,
  addProductItems,
  getProductItems,
};
